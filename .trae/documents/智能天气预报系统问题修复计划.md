## 修复计划

### 1. 日期格式化和天气类型列（DataCollection.vue + api_server.py）
- 在api_server.py中格式化日期为`YYYY-MM-DD`格式
- 在DataCollection.vue表格中添加天气类型列
- 添加日期格式化函数

### 2. 模型训练使用真实数据（api_server.py）
- 完善`/api/train-model`返回的完整评估指标
- 确保precision、recall、f1Score都从模型结果中提取
- 添加错误处理和默认值

### 3. 预报对比显示修复（ForecastComparison.vue + api_server.py）
- 统一官方预报和AI预测的日期范围
- 修复日期格式化显示
- 修复AI预测天气类型显示（处理null/undefined）
- 确保降水概率字段正确显示

### 4. 状态保持（App.vue）
- 将所有组件状态提升到App.vue
- 使用`v-show`代替`v-if`保持组件存活
- 通过props传递数据，通过events更新状态